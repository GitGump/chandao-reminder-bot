# 迭代需求群消息自动通知系统 - 测试用例文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 迭代需求群消息自动通知系统 |
| 文档版本 | v1.1 |
| 创建日期 | 2026-07-01 |
| 最后更新 | 2026-07-03 |
| 文档类型 | 测试用例文档 |
| 参考文档 | PRD v1.2 / 技术方案文档 v1.1 |

---

## 1. 发版日历管理

### 1.1 查询发版日历

| 用例编号 | TC-CAL-001 |
|----------|------------|
| 用例名称 | 获取全部发版日历 |
| 前置条件 | 数据库已有 26 条 2026 年日历数据 |
| 测试步骤 | 调用 `GET /api/calendar` |
| 预期结果 | HTTP 200，返回 26 条记录，按 planning_date ASC 排序 |

| 用例编号 | TC-CAL-002 |
|----------|------------|
| 用例名称 | 按年份筛选发版日历 |
| 前置条件 | 数据库已有 2026 年数据 |
| 测试步骤 | 调用 `GET /api/calendar?year=2026` |
| 预期结果 | HTTP 200，只返回 2026 年的记录 |

| 用例编号 | TC-CAL-003 |
|----------|------------|
| 用例名称 | 未认证时查询被拒绝 |
| 前置条件 | 无 |
| 测试步骤 | 调用 `GET /api/calendar`（不带 x-api-key） |
| 预期结果 | HTTP 401 |

| 用例编号 | TC-CAL-004 |
|----------|------------|
| 用例名称 | 数据库为空时查询 |
| 前置条件 | release_calendar 表为空 |
| 测试步骤 | `GET /api/calendar` |
| 预期结果 | HTTP 200，返回空数组 `[]` |

---

### 1.2 新增发版日历

| 用例编号 | TC-CAL-005 |
|----------|------------|
| 用例名称 | 正常新增一条日历记录 |
| 前置条件 | 无重复 |
| 测试步骤 | `POST /api/calendar`，body: `{ "year": 2026, "month": 7, "iteration_number": "0707", "planning_date": "2026-06-18", "release_date": "2026-07-07" }` |
| 预期结果 | HTTP 201，返回创建的记录 |

| 用例编号 | TC-CAL-006 |
|----------|------------|
| 用例名称 | 新增时迭代编号重复 |
| 前置条件 | 已存在 iteration_number = "0707" |
| 测试步骤 | `POST /api/calendar`，body 含 iteration_number = "0707" |
| 预期结果 | HTTP 409 |

---

### 1.3 更新发版日历

| 用例编号 | TC-CAL-007 |
|----------|------------|
| 用例名称 | 正常更新发版日期 |
| 前置条件 | id=1 存在 |
| 测试步骤 | `PUT /api/calendar/1`，body: `{ "release_date": "2026-07-08" }` |
| 预期结果 | HTTP 200 |

---

### 1.4 删除发版日历

| 用例编号 | TC-CAL-008 |
|----------|------------|
| 用例名称 | 正常删除一条记录 |
| 前置条件 | id=1 存在 |
| 测试步骤 | `DELETE /api/calendar/1` |
| 预期结果 | HTTP 204 |

---

### 1.5 CSV 批量导入

| 用例编号 | TC-CAL-009 |
|----------|------------|
| 用例名称 | 正常导入 GBK 编码 CSV 文件 |
| 前置条件 | GBK 编码 CSV（iconv-lite 解码） |
| 测试步骤 | `POST /api/calendar/import` |
| 预期结果 | HTTP 200，`{ "imported": 26, "errors": [] }` |

| 用例编号 | TC-CAL-010 |
|----------|------------|
| 用例名称 | CSV 含双引号字段 |
| 前置条件 | CSV 中某字段被 `"..."` 包裹 |
| 测试步骤 | 上传 |
| 预期结果 | HTTP 200，正确解析引号内字段 |

| 用例编号 | TC-CAL-011 |
|----------|------------|
| 用例名称 | CSV 含跨年规划会日期 |
| 前置条件 | 0106 迭代，规划会日期 = 12/19 |
| 测试步骤 | 上传 |
| 预期结果 | planning_date 自动识别为 2025-12-19（年份-1） |

| 用例编号 | TC-CAL-012 |
|----------|------------|
| 用例名称 | 上传非 CSV 文件 |
| 前置条件 | 上传 .xlsx 文件 |
| 测试步骤 | 上传 |
| 预期结果 | HTTP 400 |

| 用例编号 | TC-CAL-013 |
|----------|------------|
| 用例名称 | 文件大小超过 1MB |
| 前置条件 | CSV 文件 > 1MB |
| 测试步骤 | 上传 |
| 预期结果 | HTTP 400，返回文件过大提示 |

---

## 2. 机器人配置

### 2.1 查询机器人

| 用例编号 | TC-ROB-001 |
|----------|------------|
| 用例名称 | 获取机器人列表 |
| 前置条件 | 有机器人记录 |
| 测试步骤 | `GET /api/robot` |
| 预期结果 | HTTP 200，返回含 id/name/webhook_url/is_active |

---

### 2.2 新增机器人

| 用例编号 | TC-ROB-002 |
|----------|------------|
| 用例名称 | 正常新增机器人 |
| 前置条件 | 无 |
| 测试步骤 | `POST /api/robot`，含 name 和 webhook_url |
| 预期结果 | HTTP 201，is_active = 1（SQLite 模式） |

---

### 2.3 删除机器人

| 用例编号 | TC-ROB-003 |
|----------|------------|
| 用例名称 | 正常删除机器人 |
| 前置条件 | id=1 存在 |
| 测试步骤 | `DELETE /api/robot/1` |
| 预期结果 | HTTP 204 |

| 用例编号 | TC-ROB-004 |
|----------|------------|
| 用例名称 | 测试机器人 Webhook 连通性 |
| 前置条件 | Webhook URL 有效 |
| 测试步骤 | `POST /api/robot/1/test` |
| 预期结果 | HTTP 200，`{ "success": true }` |

---

## 3. 消息模板

| 用例编号 | TC-TPL-001 |
|----------|------------|
| 用例名称 | 获取所有消息模板 |
| 前置条件 | 种子数据已有 3 条 |
| 测试步骤 | `GET /api/templates` |
| 预期结果 | HTTP 200，返回 3 条，按 message_type 排序 |

| 用例编号 | TC-TPL-002 |
|----------|------------|
| 用例名称 | 更新已存在的模板（Upsert） |
| 前置条件 | message_type=1 已有模板 |
| 测试步骤 | `POST /api/templates`，body: `{ "message_type": 1, "template_content": "新模板" }` |
| 预期结果 | HTTP 200，template_content 更新 |

| 用例编号 | TC-TPL-003 |
|----------|------------|
| 用例名称 | 模板保存后前端正确提示 |
| 前置条件 | 无 |
| 测试步骤 | 编辑模板 → 点击保存 |
| 预期结果 | Toast 提示"模板已保存" |

---

## 4. 推送时间配置

| 用例编号 | TC-PT-001 |
|----------|------------|
| 用例名称 | 获取推送时间配置 |
| 前置条件 | 数据库有 3 条默认配置 |
| 测试步骤 | `GET /api/push-time` |
| 预期结果 | HTTP 200，含 robot_config 联表数据 |

| 用例编号 | TC-PT-002 |
|----------|------------|
| 用例名称 | 更新推送时间（Upsert） |
| 前置条件 | 无 |
| 测试步骤 | `POST /api/push-time`，`{ "message_type": 1, "robot_id": 1, "hour": 9, "minute": 30 }` |
| 预期结果 | HTTP 200，配置更新 |

| 用例编号 | TC-PT-003 |
|----------|------------|
| 用例名称 | 手���推送按钮触发推送 |
| 前置条件 | 绑定机器人和模板均已配置 |
| 测试步骤 | 点击「手动推送」按钮 |
| 预期结果 | push-now API 调用成功，日志记录 push_source="manual" |

---

## 5. @成员配置

| 用例编号 | TC-MEM-001 |
|----------|------------|
| 用例名称 | 新增成员（仅输入姓名） |
| 前置条件 | 无 |
| 测试步骤 | `POST /api/members`，`{ "message_type": 1, "member_name": "张三" }` |
| 预期结果 | HTTP 201，userid 自动 = "张三" |

| 用例编号 | TC-MEM-002 |
|----------|------------|
| 用例名称 | 编辑成员名称后 userid 自动同步 |
| 前置条件 | 已存在成员 name="张三", userid="张三" |
| 测试步骤 | `PUT /api/members`，`{ "id": 1, "member_name": "李四" }` |
| 预期结果 | HTTP 200，userid 自动更新为 "李四" |

| 用例编号 | TC-MEM-003 |
|----------|------------|
| 用例名称 | 删除成员（自定义确认弹窗） |
| 前置条件 | id=1 存在 |
| 测试步骤 | 点击删除 → 弹窗确认 → 点「删除」 |
| 预期结果 | 成员删除，列表刷新，Toast 提示 |

| 用例编号 | TC-MEM-004 |
|----------|------------|
| 用例名称 | 删除确认弹窗取消操作 |
| 前置条件 | id=1 存在 |
| 测试步骤 | 点击删除 → 弹窗点「取消」 |
| 预期结果 | 弹窗关闭，成员未被删除 |

| 用例编号 | TC-MEM-005 |
|----------|------------|
| 用例名称 | 消息推送正确使用最新成员名称 |
| 前置条件 | 成员 name 从 "张三" 改为 "李四" |
| 测试步骤 | 触发推送 |
| 预期结果 | 消息含 `@李四`，不含 `@张三` |

---

## 6. 推送日志

| 用例编号 | TC-LOG-001 |
|----------|------------|
| 用例名称 | 查询全部推送日志 |
| 前置条件 | 有日志记录 |
| 测试步骤 | `GET /api/push-logs` |
| 预期结果 | HTTP 200，`{ "logs": [...], "total": N }` |

| 用例编号 | TC-LOG-002 |
|----------|------------|
| 用例名称 | 日志区分推送来源（自动/手动） |
| 前置条件 | 有 auto 和 manual 来源的日志 |
| 测试步骤 | 查看推送日志 Tab |
| 预期结果 | 来源列显示"自动"（蓝色）或"手动"（琥珀色） |

| 用例编号 | TC-LOG-003 |
|----------|------------|
| 用例名称 | 失败日志重试 |
| 前置条件 | id=1 日志 status=failed |
| 测试步骤 | `POST /api/push-logs/1/retry` |
| 预期结果 | 重新发送，日志更新 |

| 用例编号 | TC-LOG-004 |
|----------|------------|
| 用例名称 | 重试已成功的推送被拒绝 |
| 前置条件 | id=1 日志 status=success |
| 测试步骤 | `POST /api/push-logs/1/retry` |
| 预期结果 | HTTP 400，"只能重试失败的推送" |

| 用例编号 | TC-LOG-005 |
|----------|------------|
| 用例名称 | 日志详情弹窗显示 push_source |
| 前置条件 | 有手动推送的日志 |
| 测试步骤 | 点击日志行 |
| 预期结果 | 详情弹窗显示 "来源: 手动" |

---

## 7. 消息触发（核心流程）

### 7.1 自动触发推送

| 用例编号 | TC-TRIG-001 |
|----------|------------|
| 用例名称 | 正常触发规划会提醒（使用下一迭代） |
| 前置条件 | 当前日期 7/3，当前迭代 0707，下一迭代 0721（planning=7/3） |
| 测试步骤 | `POST /api/trigger`，`{ "message_type": 1 }` |
| 预期结果 | 使用 0721 迭代数据，而非 0707 |

| 用例编号 | TC-TRIG-002 |
|----------|------------|
| 用例名称 | 正常触发进度更新提醒（使用当前迭代） |
| 前置条件 | 当前迭代 0707 |
| 测试步骤 | `POST /api/trigger`，`{ "message_type": 2 }` |
| 预期结果 | 使用 0707 迭代数据 |

| 用例编号 | TC-TRIG-003 |
|----------|------------|
| 用例名称 | 手动推送记录 push_source="manual" |
| 前置条件 | 配置完整 |
| 测试步骤 | `POST /api/push-now`，`{ "message_type": 1 }` |
| 预期结果 | 日志 push_source = "manual" |

| 用例编号 | TC-TRIG-004 |
|----------|------------|
| 用例名称 | 自动推送记录 push_source="auto" |
| 前置条件 | 配置完整 |
| 测试步骤 | Cron Worker 触发 |
| 预期结果 | 日志 push_source = "auto" |

---

### 7.2 占位符替换

| 用例编号 | TC-TRIG-005 |
|----------|------------|
| 用例名称 | {发版日日期} 正确渲染 |
| 前置条件 | 当前迭代发版日为 7/7 |
| 测试步骤 | 触发推送 |
| 预期结果 | 替换为 `7/7` |

| 用例编号 | TC-TRIG-006 |
|----------|------------|
| 用例名称 | {发版日日期+3} 正���计算 |
| 前置条件 | 发版日 7/7 |
| 测试步骤 | 触发推送 |
| 预期结果 | 替换为 `7/10` |

| 用例编号 | TC-TRIG-007 |
|----------|------------|
| 用例名称 | {发版日日期所在星期} 正确渲染 |
| 前置条件 | 2026-07-07 是周二 |
| 测试步骤 | 触发推送 |
| 预期结果 | 替换为 `周二` |

| 用例编号 | TC-TRIG-008 |
|----------|------------|
| 用例名称 | 占位符替换顺序正确 |
| 前置条件 | 模板同时含 {发版日日期} 和 {发版日日期+3} |
| 测试步骤 | 触发推送 |
| 预期结果 | 先替换 {发版日日期+3}，再替换 {发版日日期}，无误伤 |

| 用例编号 | TC-TRIG-009 |
|----------|------------|
| 用例名称 | 模板内容为空时拒绝 |
| 前置条件 | template_content = "" |
| 测试步骤 | 触发推送 |
| 预期结果 | HTTP 400，"模板内容为空" |

| 用例编号 | TC-TRIG-010 |
|----------|------------|
| 用例名称 | 禅道编号防御为负数 |
| 前置条件 | 计算出的编号 ≤ 0 |
| 测试步骤 | 触发推送 |
| 预期结果 | 编号防御为 1，不出现负数 |

---

## 8. 下次推送时间计算

| 用例编号 | TC-NPT-001 |
|----------|------------|
| 用例名称 | 规划会提醒使用下一迭代 |
| 前置条件 | 当前日期 7/3，下一迭代 0721（planning=7/3） |
| 测试步骤 | `GET /api/next-push-times` |
| 预期结果 | 类型1: iteration="0721" |
| 类型2: iteration="0707" |
| 类型3: iteration="0707" |

| 用例编号 | TC-NPT-002 |
|----------|------------|
| 用例名称 | 返回字段与前端匹配 |
| 前置条件 | 无 |
| 测试步骤 | `GET /api/next-push-times` |
| 预期结果 | 每条含 `next_time` 和 `iteration` 字段 |

| 用例编号 | TC-NPT-003 |
|----------|------------|
| 用例名称 | 周末自动顺延 |
| 前置条件 | 计算日期为周六 |
| 测试步骤 | `GET /api/next-push-times` |
| 预期结果 | 顺延到周一 |

---

## 9. 手动推送 API

| 用例编号 | TC-PN-001 |
|----------|------------|
| 用例名称 | x-api-key 认证正常 |
| 前置条件 | 无 |
| 测试步骤 | `POST /api/push-now` 带 x-api-key |
| 预期结果 | HTTP 200，推送成功 |

| 用例编号 | TC-PN-002 |
|----------|------------|
| 用例名称 | x-trigger-key 不能访问 push-now |
| 前置条件 | 无 |
| 测试步骤 | `POST /api/push-now` 带 x-trigger-key |
| 预期结果 | HTTP 401 |

| 用例编号 | TC-PN-003 |
|----------|------------|
| 用例名称 | push-now 内部使用 trigger-key 调用 trigger |
| 前置条件 | 无 |
| 测试步骤 | `POST /api/push-now`，`{ "message_type": 1 }` |
| 预期结果 | trigger 接收到 push_source="manual" |

---

## 10. SQLite 兼容层

| 用例编号 | TC-SQL-001 |
|----------|------------|
| 用例名称 | 自动建表（幂等） |
| 前置条件 | 无 data/local.db |
| 测试步骤 | 启动 `npm run dev` |
| 预期结果 | 所有 11 张表自动创建，种子数据插入 |

| 用例编号 | TC-SQL-002 |
|----------|------------|
| 用例名称 | boolean 值自动转换 |
| 前置条件 | 写入 `{ "is_active": true }` |
| 测试步骤 | 任意写入含 boolean 的 API |
| 预期结果 | 数据库存储为 1，读取时前端正确解析 |

| 用例编号 | TC-SQL-003 |
|----------|------------|
| 用例名称 | single() 兼容单对象和数组 |
| 前置条件 | INSERT 和 SELECT 操作 |
| 测试步骤 | 新增机器人 → 查询单个 |
| 预期结果 | 两者都正确返回对象 |

| 用例编号 | TC-SQL-004 |
|----------|------------|
| 用例名称 | select() 不覆盖 INSERT/UPDATE/DELETE |
| 前置条件 | 无 |
| 测试步骤 | POST /api/robot → 查询 |
| 预期结果 | 数据正确写入，不会因 select() 丢失操作类型 |

---

## 11. 权限验证

| 用例编号 | TC-AUTH-001 |
|----------|------------|
| 用例名称 | x-api-key 错误时拒绝 |
| 前置条件 | 无 |
| 测试步骤 | 带错误 key 访问 /api/calendar |
| 预期结果 | HTTP 401 |

| 用例编号 | TC-AUTH-002 |
|----------|------------|
| 用例名称 | trigger 拒绝 x-api-key |
| 前置条件 | 无 |
| 测试步骤 | 带 x-api-key 访问 /api/trigger |
| 预期结果 | HTTP 401 |

| 用例编号 | TC-AUTH-003 |
|----------|------------|
| 用例名称 | health 无需认证 |
| 前置条件 | 无 |
| 测试步骤 | `GET /api/health` 无 Header |
| 预期结果 | HTTP 200 |

---

## 12. 前端 UI 测试

| 用例编号 | TC-UI-001 |
|----------|------------|
| 用例名称 | 推送时间 Tab 显示手动推送按钮 |
| 前置条件 | 页面加载 |
| 测试步骤 | 切换到推送时间 Tab |
| 预期结果 | 每个消息类型卡片底部有绿色「手动推送」按钮 |

| 用例编号 | TC-UI-002 |
|----------|------------|
| 用例名称 | 推送日志显示来源列 |
| 前置条件 | 有推送日志 |
| 测试步骤 | 切换到推送日志 Tab |
| 预期结果 | 来源列显示"自动"或"手动"Badge |

| 用例编号 | TC-UI-003 |
|----------|------------|
| 用例名称 | @成员页面仅输入姓名 |
| 前置条件 | 页面加载 |
| 测试步骤 | 添加成员 |
| 预期结果 | 表单只含姓名输入框，无 UserID 输入框 |

| 用例编号 | TC-UI-004 |
|----------|------------|
| 用例名称 | 编辑成员后推送使用新名称 |
| 前置条件 | 已添加成员 |
| 测试步骤 | 编辑名称 → 手动推送 |
| 预期结果 | 消息 @ 使用新名称 |

| 用例编号 | TC-UI-005 |
|----------|------------|
| 用例名称 | 删除确认弹窗正确工作 |
| 前置条件 | 有成员 |
| 测试步骤 | 点击垃圾桶图标 |
| 预期结果 | 弹窗显示成员姓名 + 取消/删除按钮 |

---

## 测试用例统计

| 模块 | 用例数 |
|------|--------|
| 发版日历管理 | 13 |
| 机器人配置 | 4 |
| 消息模板 | 3 |
| 推送时间配置 | 3 |
| @成员配置 | 5 |
| 推送日志 | 5 |
| 消息触发 | 10 |
| 下次推送时间 | 3 |
| 手动推送 API | 3 |
| SQLite 兼容层 | 4 |
| 权限验证 | 3 |
| 前端 UI | 5 |
| **合计** | **61** |

---

## 文档变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-07-01 | 初始版本，覆盖 17 个模块 148 条用例 |
| v1.1 | 2026-07-03 | 精简为核心用例 61 条，新增 push_source/手动推送/规划会优化/@成员简化/SQLite 兼容层用例，移除已废弃特性 |

---

*文档结束*
